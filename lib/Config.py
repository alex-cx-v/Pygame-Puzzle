# Define the width of the application window
WINDOW_WIDTH = 800

# Define the height of the application window
WINDOW_HEIGHT = 750

# Combine width and height into a tuple representing the window size
WINDOW_SIZE = (WINDOW_WIDTH, WINDOW_HEIGHT)